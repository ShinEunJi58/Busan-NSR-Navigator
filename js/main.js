// 페이지 로드 시 실행
document.addEventListener('DOMContentLoaded', function () {
    // 차트 초기화
    initCharts();

    // 초기 계산 실행
    calculate();

    console.log('🚢 북극항로 스마트 네비게이터 로드 완료!');
});
